export const UI_THEME = {
  colors: {
    background: {
      page: 'var(--bg-page)',
      panel: 'var(--bg-panel)'
    },
    text: {
      primary: 'var(--text-primary)',
      secondary: 'var(--text-secondary)'
    }
  }
}
